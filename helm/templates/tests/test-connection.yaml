apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "votingApp.fullname" . }}-test-connection"
  labels:
    {{- include "votingApp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "votingApp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
